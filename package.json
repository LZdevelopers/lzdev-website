{
  "name": "lzdev-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Website institucional da LZdev - desenvolvimento de sites e sistemas web sob medida",
  "scripts": {
    "dev": "vite",
    "build": "npm run seo && vite build && vite build --ssr src/entry-server.jsx --outDir dist-ssr && node scripts/prerender.mjs",
    "preview": "vite preview",
    "check": "node scripts/check-seo.mjs",
    "seo": "node scripts/generate-seo.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "og": "node scripts/generate-og.mjs",
    "fonts": "node scripts/copy-fonts.mjs",
    "assets": "npm run icons && npm run og && npm run fonts"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.5",
    "@fontsource-variable/plus-jakarta-sans": "^5.2.5",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "@vitejs/plugin-react": "^5.0.0",
    "tailwindcss": "^4.1.11",
    "vite": "^7.0.0"
  }
}
